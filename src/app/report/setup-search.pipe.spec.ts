import { SetupSearchPipe } from './setup-search.pipe';

describe('SetupSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SetupSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
